#include <fstream>
#include <iostream>
#include <vector>
using namespace std;

int main() {
  int N;
  vector<int> node_list;
  vector<int> node;

  ifstream infile("input1.txt");
  infile >> N;

  for (int i = 0; i < N; i++) {
  }

  return 0;
}